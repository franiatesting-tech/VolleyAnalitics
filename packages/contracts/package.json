{
  "name": "@volley/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript types + typed client generated from services/api's OpenAPI schema. Never hand-write duplicate request/response types elsewhere -- import from here.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "export-openapi": "node scripts/export-openapi.mjs",
    "gen-types": "openapi-typescript openapi.json -o src/schema.d.ts",
    "generate": "pnpm run export-openapi && pnpm run gen-types",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  }
}
