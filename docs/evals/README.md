# Evals

Frozen-dataset evaluation reports for models before they're promoted to baseline — see `.claude/skills/ml-evaluation` for the bar each report must clear (baseline comparison, error/slice analysis, reproducibility).
