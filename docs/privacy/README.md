# Privacy

Where client-data-handling policy lives: what's collected, retention, `TRAINING_OPT_IN` semantics (off by default, per organization — see `docs/architecture/ML_PIPELINE.md`), and org-isolation guarantees. Populate before Phase 6 (first real client video) per `ROADMAP.md`. Reviewed by the `security-privacy-license-reviewer` agent.
