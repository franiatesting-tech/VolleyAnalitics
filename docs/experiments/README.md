# Experiments

Human-readable summaries of significant MLflow experiment runs — the "why" behind a model/config change, written for someone who won't open MLflow. Not a replacement for MLflow's own tracking (git commit, dataset version, weights hash, config, seed, metrics, artifacts — see `.claude/skills/cv-experiment`), just the narrative layer on top.
