[project]
name = "volley-intelligence"
version = "0.0.0"
requires-python = ">=3.11,<3.12"

[tool.uv.workspace]
members = [
  "packages/domain-py",
  "packages/storage-py",
  "services/api",
  "services/worker",
]

[dependency-groups]
# ruff itself isn't a dependency of any workspace member -- CI's `uv run
# ruff check .`/`uv run ruff format --check .` run at the workspace root,
# so the tool has to be declared here (mirrors ml/pyproject.toml's own
# identical dev group).
dev = [
  "ruff>=0.12",
]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["ml", "tools"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
# FastAPI's Depends(...)-as-default-argument is the framework's documented,
# correct idiom -- not the "mutable default argument" footgun B008 targets.
"services/api/src/volley_api/api/routes/*.py" = ["B008"]
"services/api/src/volley_api/core/auth.py" = ["B008"]

[tool.pytest.ini_options]
asyncio_mode = "strict"
