# Project Status

_Last updated: 2026-08-29_

## Phase

**Phase 3 — Sports analytics design system (frontend): DONE.** Built, reviewed by two independent specialists, fixed, and re-confirmed by both in a second targeted pass — architecture-lead's verdict: *"Phase 3 is sound."* qa-release-engineer's verdict: *"The definition-of-done bar for Phase 3 is met."* Phases 0–2 (foundation, platform skeleton, volleyball domain ontology) are done — see below. This session built `apps/web`'s Match Analysis / Rally Explorer against the real Phase 2 ontology endpoints, a reusable 2D top-down tactical court component, and rally replay.

Round 1: architecture-lead + qa-release-engineer reviewed in parallel, found 4 blocking issues + 11 should-fix/nice-to-have findings (2 independently confirmed by both). Round 2 (after fixes landed): both re-checked their own findings against the actual running code and re-ran every test suite themselves rather than trusting the report — architecture-lead re-verified all 4 blocking items line-by-line and caught that its *own* originally-suggested design for one sub-fix had a real bug (would silently fail on an ordinary 3-0 sweep) that the implementer had already caught and correctly overridden; qa-release-engineer live-clicked through all 7 of its original findings against the running app (including an exact-count check: a zone click surfaced exactly 54 events matching the displayed "54" count). 4 small residual polish items from architecture-lead's re-check are fixed too. See "Phase 3 independent review" below for full detail.

## Phase 2 — Volleyball domain ontology, Event Log, statistics engine, lineage: done

Phase 0 (architecture/governance) and Phase 1 (platform skeleton — API/worker/web, independently reviewed and all blocking findings fixed) are done. This session built the real volleyball ontology per `docs/domain/ONTOLOGY.md` / ADR-004: 21 new tables, a pure statistics engine, a coordinate system, a lineage helper, an append-only correction service, wired the Phase 1 synthetic generator to persist into it for real (not just the JSON blob) — and both independent reviews this phase required (volleyball-domain-analyst, architecture-lead) have run, and every blocking finding from both is fixed and re-verified. Four should-fix findings remain deliberately deferred with tracked `TECH_DEBT.md` entries (see "Open risks carried forward" below) — not silently dropped.

## What exists and is verified (real commands run, not just claimed)

- **`docs/domain/ONTOLOGY.md`** — full entity documentation, ER diagram (mermaid), design decisions (why there's no generic `Prediction` table, why `CourtPosition` is a value type not a table), correction semantics, statistics-engine formulas verified against SDHSAA/NCAA/coachingvb.com sources (not assumed).
- **`packages/domain-py/src/volley_domain/ontology.py`** — Season/Competition/Team/Player/Roster, Lineup/LineupPlayer/Rotation, MatchSet/Rally/Phase/Action/Outcome, Video/VideoAsset/PipelineRun/ModelRun, BallObservation/PlayerObservation, HumanCorrection/ReviewedLabel. Split from Phase 1's `models.py` via a new shared `base.py` (see ADR-004). `Action.model_run_id` is `NOT NULL`/`CASCADE` (provenance always required, per architecture review); `MatchSet`/`Rally`/`Phase`/`Action` have `UniqueConstraint`s on their ordering columns; `Video.video_hash` uniqueness is org-scoped, not global.
- **`volley_domain.court`** — normalized coordinate system, zone anchors, mirroring, nearest-zone attribution. 12 geometric tests. Mirroring is exercised only by rendering code, never applied when writing ground truth (see the away-team zone-attribution fix below).
- **`volley_domain.stats`** — pure statistics engine (serves/aces/errors, configurable-scale reception rating, attack efficiency including tips, blocks, digs, sideout/breakpoint %, setter distribution, rally duration). 27 known-input/known-output tests, including the exact SDHSAA 20/5/30→0.50 worked example.
- **`volley_domain.lineage`** — `explain_metric`, a real, tested pure function. **Not** wired to a real caller yet — see "Open risks" below.
- **`volley_domain.corrections`** — append-only `HumanCorrection`/`ReviewedLabel` recording, verified against a real (SQLite) database that a correction never destroys the prediction it corrects. 4 tests.
- **`volley_domain.synthetic.persistence`** — the synthetic generator now writes real ontology rows (not just JSON), verified end-to-end: 7 tests proving correct row counts, no cross-team phase contamination, in-bounds coordinates, deterministic output, and match-absolute (not rally-clustered) timestamps.
- **`services/worker`** — `process_demo_match` now persists into the real ontology *and* still writes the JSON blob (deliberate coexistence, see ADR-004/TECH_DEBT.md), atomically, with a test proving Celery redelivery does not duplicate ontology rows.
- **`services/api`** — 4 new org-scoped read endpoints (`/matches/{id}/sets`, `/rallies`, `/rallies/{id}/actions`, `/matches/{id}/statistics`), tests against real persisted data, including that every one 404s for a cross-tenant request.
- **Migration `0002`** — creates all 21 new tables (+ new nullable columns on `matches`/`processing_jobs`) as **frozen, hand-owned `op.create_table()` DDL**, not read from live SQLAlchemy metadata at migration-run time (an earlier draft did that; rewritten after architecture review — see ADR-004). **Verified 2026-08-29 against a real Postgres 16** (`docker compose up postgres`, `alembic upgrade head`): both `0001` and `0002` apply cleanly, all 24 tables (2 Phase-1 + 21 Phase-2 + `alembic_version`) exist with the expected FKs/unique constraints (spot-checked `actions`: `uq_action_phase_index`, `model_run_id NOT NULL` + `ON DELETE CASCADE`, all as designed). Downgrade direction is still only offline-verified (`--sql`) — a live-Postgres downgrade run was blocked by the repo's own destructive-command guard, which requires explicit confirmation before running `alembic downgrade` against a real database; not attempted since the upgrade path was the actual open risk.
- **`docs/domain/examples/`** — real JSON examples (Action, Rally, HumanCorrection), generated by actually running the pipeline. `derived_metric_lineage.json` is the one exception — see its corrected `README.md` entry: it illustrates `explain_metric`'s output shape from hand-constructed inputs, since no real caller assembles those inputs yet.
- **Total: 98/98 Python tests passing** (65 `domain-py` + 27 `api` + 6 `worker`, run per-package — a pre-existing `conftest.py` module-name collision between `services/api/tests` and `services/worker/tests` means they cannot currently be collected in one combined `pytest` invocation; not a new issue, not yet fixed, not blocking since CI/local workflows run them separately). Ruff and `ruff format --check` clean across the whole Python codebase. **pyright was not run this session — not installed in this sandbox and no network install was attempted; this is a real gap, not a "clean" claim being made falsely.**

## Phase 3 — what exists and is verified (real browser, real backend, not just typecheck)

- **Design system**: Phase 1's graphite/dark-neutral + electric-accent theme (`apps/web/src/app/globals.css`) was already largely in place and consistent across auth/matches/org pages; extended with a `Tabs` primitive (`components/ui/tabs.tsx`, Radix-based) and mono/tabular-nums treatment for stat numbers. No changes needed to the existing token set — it already matched the sports-dataviz mandate.
- **`TacticalCourt`** (`components/court/tactical-court.tsx`) — reusable SVG+D3(scales-only) 2D court: zone grid, net, attack lines, player tokens, ball marker with provenance-based styling, trajectory trace, zone heatmap markers. Every marker carries source ids for click-through. Used by both rally replay and the statistics dashboard's per-team attack zone maps (`components/stats/zone-map.tsx`) — one primitive, two real consumers.
- **Coordinate geometry** (`lib/court-geometry.ts`) — ported `ZONE_ANCHORS`/`mirror_for_away` from `volley_domain.court`, plus a new `toFullCourtFrame` transform (home unmirrored/bottom half, away x-reflected/top half — reasoned from first principles since the two teams face opposite directions across the net; documented inline, tested in `court-geometry.test.ts`). This is a frontend-only rendering decision, not a change to backend zone semantics.
- **Match Analysis** (`components/match-analysis.tsx`), rebuilt against `/matches/{id}/sets`, `/rallies`, `/rallies/{id}/actions`, `/matches/{id}/statistics` — Overview/Statistics/Rally Explorer tabs. Verified end-to-end against a live match run through the real pipeline (Postgres + FastAPI + Celery worker, not mocked): real set scores, real per-team statistics (serve/reception/attack/block/dig/sideout/breakpoint/rally duration), all null-safe (reception rating shows "—" when `quality_rating` is never populated by the generator, rather than a fake 0).
- **Statistic → Events click-through**: implemented as a real lookup (`lib/ontology.ts`'s `matchesStatCategory`), not a backend re-derivation — `lineage.explain_metric` still isn't wired to any API route (carried open risk, see below), so the frontend locates matching `Action` rows itself via the already-fetched-per-rally actions and links each to its source rally. Verified live: clicking "Aces: 9" surfaced exactly 9 matching serve events, each linking into Rally Explorer with the correct rally selected and highlighted.
- **Rally Explorer** (`components/rallies/rally-explorer.tsx`) — rally list (208 rallies across 5 sets, from real `Rally` rows) → ordered real Action/Outcome timeline (`rally-timeline.tsx`) → animated 2D replay (`rally-replay.tsx`) sourced from `GET /matches/{id}/result`'s position time series, paired to the selected real `Rally` by `(set index, index_in_set)` — never by id (see TECH_DEBT.md). Verified live: play/pause, scrub, and click-a-timeline-action-to-seek-the-replay all work and stay in sync; the video slot honestly reads "Video not available yet — synthetic reconstruction shown," never implying real footage.
- **`prefers-reduced-motion`**: respected globally (existing `globals.css` rule) plus a `useReducedMotion` hook (`useSyncExternalStore`-based, no effect-setState lint violation) surfaced in the replay UI; playback still functions under reduced motion (it communicates real elapsed match time, not decoration) but CSS position transitions are suppressed.
- **Loading/error/empty states**: present for every new data-fetching surface (sets/rallies/statistics/actions/result — pending/error/empty all handled, `data-testid`s follow the existing convention). Verified by triggering both the pre-processing (draft/processing) and post-processing (completed) states live.
- **Tests**: 21 new Vitest tests (`ontology.test.ts`, `court-geometry.test.ts`, `rally-replay.test.ts`) covering team-side resolution, rally pairing, stat-category matching, and the full-court coordinate transform/interpolation math — pure-logic unit tests, not snapshot tests. Full web suite: 33/33 passing, **re-confirmed independently** (`pnpm lint`/`pnpm typecheck`/`pnpm test` all re-run clean, not just trusted from the agent's report).
- **A real, significant bug found by the agent, and fixed in this session's independent pass**: `synthetic/generator.py`'s `_build_action_chain` rolled an independent `rally_shape` random choice for the "ace"/"serve_error" short rally paths, ignoring the `point_winner` the caller already decided — so a rally's `Rally.point_winner_team_id` could (and empirically did, ~69% of the 29 checked single-serve rallies in one generated match) contradict its own recorded Action/Outcome. The frontend agent correctly identified this as a real bug outside its own `packages/domain-py`-is-off-limits scope and flagged it rather than touching that file. **Fixed 2026-08-29**: the ace/serve_error rally shape is now only offered when consistent with the already-decided `point_winner` (verified by re-deriving the exact contradiction from the code before fixing, not just trusting the report). Regression test added: `test_rally_point_winner_agrees_with_its_own_final_action_outcome` (30 seeds). Full `domain-py` suite: 66/66 passing, ruff clean.
- **Deviation from the brief**: `MatchOut` (services/api) doesn't expose `home_team_id`/`away_team_id`, so team-side ("home"/"away") resolution for the ontology's team-id-keyed data had no direct API answer. Worked around in the frontend by anchoring on the synthetic blob's `serving_team` label via the same rally-pairing key (see `deriveTeamSides` in `lib/ontology.ts`) — works today, but will silently stop resolving once real (non-synthetic) matches exist without a JSON blob. Documented as a new TECH_DEBT.md entry with the real fix (add the two columns to `MatchOut`) flagged for architecture-lead, not attempted here since it's a `packages/contracts`-boundary change out of this phase's authority.
- **A separate, pre-existing infrastructure gap found during independent verification (unrelated to this phase's frontend work, dates to Phase 1)**: `packages/ui` is a declared pnpm workspace member (`pnpm-workspace.yaml`) and `infra/docker/web.Dockerfile` explicitly `COPY`s its `package.json` before install, but the directory only ever had a `.gitkeep` — no `package.json` ever existed. This means `docker compose up --build` for the `web`/`web-migrate` services has likely never actually succeeded since Phase 1 (nobody had run it — prior sessions used `pnpm dev`/`uv run` directly on the host). **Fixed 2026-08-29**: added a minimal, honest placeholder `packages/ui/package.json` (nothing lives there yet; `apps/web` still owns all its own components).
- **Another pre-existing gap found (not a Phase 3 regression, not fixed)**: running `services/api` directly on a Windows host via the bare `uvicorn` CLI (i.e. not through Docker) 500s on any route touching the async DB session — psycopg's async driver is incompatible with Windows' default `ProactorEventLoop`. A `sys.platform == "win32"` guard setting `WindowsSelectorEventLoopPolicy` in `main.py` was tried and reverted: it doesn't work, because uvicorn's CLI creates its event loop and starts running *before* it imports the ASGI app string, so by the time `main.py`'s module-level code runs the wrong loop already exists — the real fix needs a custom entrypoint script that sets the policy before calling `uvicorn.run()` programmatically, which is more than this finding warranted fixing in the moment. Not a problem in the project's actual supported dev path (`docker compose`, which runs Linux containers) or CI (`ubuntu-latest`) — only bare-metal Windows host execution of `services/api` is affected. Confirmed live: this session's actual browser verification below was done with `api`/`worker` running as Docker containers (images already built successfully) against the host's `pnpm dev` Next.js server, not via raw `uvicorn`.
- **Live browser re-verification, this session, independent of the agent's own report**: with the full stack up for real (`docker compose` `postgres`/`valkey`/`api`/`worker` + host `pnpm dev` for `web`), clicked through the actual running app against a real match with 208 persisted rallies: Overview tab (real set scores 3–2), Statistics tab (both teams' full stat breakdown), clicked the "Aces" tile and got exactly 9 source events listed, clicked one into Rally Explorer and confirmed the correct rally opened with its replay controls and 2D court rendering both teams' player tokens either side of a labeled net, confirmed the "Video not available yet" honest placeholder text, and confirmed mobile viewport (375×812) renders without layout breakage. One of the aces clicked through to happened to be *pre-fix* demo data and visibly showed the exact point_winner/outcome contradiction described above — a live, concrete confirmation the bug was real, not a live bug in the current code (the match was processed before the fix landed).

## Phase 3 independent review: both complete, findings below, all blocking + cross-confirmed items fixed

Two independent reviews ran in parallel (architecture-lead + qa-release-engineer, neither aware of the other's findings), then a third pass (this session, not either reviewer) fixed what they found and re-verified live. Full findings live in each review's transcript; summarized here so this file stays the single source of truth for what was found and what's still open.

**architecture-lead verdict**: *"The two decisions this phase reasoned through are both correct... What is missing is the last mile of rigour."* 4 blocking, 7 should-fix, 4 nice-to-have.

**qa-release-engineer verdict**: mechanical bar (lint/typecheck/tests/license gate/loading-error-empty/mobile/reduced-motion/top-level click-through) genuinely clean, re-verified live against a real running app — but 2 findings that fail the sports-dataviz click-through mandate and the release-gate skill's scope-consistency requirement. 1 should-fix (independently confirmed by architecture-lead too), 1 should-fix (dormant a11y gap), 1 should-fix (a different, newly-surfaced field), 1 should-fix (independently confirmed by architecture-lead too), 2 nice-to-have.

**Blocking, all fixed 2026-08-29:**
1. **CI-breaking** (architecture-lead): `apps/web/e2e/smoke.spec.ts` asserted on `data-testid="match-result"`/`"set-list"`, both deleted by this phase's rewrite. **Fixed**: rewritten to assert on `match-analysis-tabs`/`set-score-list`, plus a new step exercising the phase's headline capability (click a stat tile → confirm `stat-events-panel`/`stat-events-list` show real matching events). Type-checked and linted; not run end-to-end in this pass (would need the full stack + Better Auth migrations spun up specifically for Playwright — the underlying UI flow it now asserts on was already manually verified live, see below).
2. **Violated a CLAUDE.md fixed decision** (architecture-lead): `ballTrajectoryFullCourt` dropped each ball sample's `provenance` before drawing the trace, so interpolated/predicted points rendered identically to real observations. **Fixed**: `provenance` now flows through to `TacticalCourt`, which draws the trace as separate solid/dashed/dotted segments per contiguous same-provenance run, plus a new in-SVG legend. Verified live: legend text ("Observed"/"Interpolated") confirmed present in the running replay UI.
3. **The documented `MatchOut` fix was factually wrong** (architecture-lead): nothing ever wrote `Match.home_team_id`/`away_team_id`. **Fixed** per architecture-lead's 3-step plan: `persist_synthetic_match` now links the `Match` row to its `Team` rows (new regression test: `test_persist_synthetic_match_links_match_to_its_home_and_away_teams`); `MatchOut` now exposes both fields (nullable); `deriveTeamSides` rewritten as a 3-tier fallback (real `MatchOut` ids → derive from `MatchSet.winner_team_id`+points, using rallies to discover both team ids so a 3-0 sweep still resolves → old blob-pairing as last resort). `packages/contracts` regenerated. Verified live against the existing pre-fix demo match: Overview/Statistics both still resolve correctly via the new tier-2/3 fallback.
4. **Overview tab guessed wrong instead of abstaining** (architecture-lead): silently defaulted every set's winner label to "away" when team sides couldn't be resolved. **Fixed**: shows "—" instead of guessing, matching how Statistics/headline already behaved.

**Should-fix, fixed 2026-08-29 (prioritized: cross-confirmed by both reviews, or cheap):**
- **Zone-map click-through was dead code** (qa-release-engineer finding #1, independently confirmed by architecture-lead's S-3): `TacticalCourt`'s `sourceActionIds`/`onSelect` props existed but neither real consumer (`zone-map.tsx`, `rally-replay.tsx`) ever passed them, so the attack-zone-distribution heatmap's per-zone numbers had no click-through despite the component's own docstring claiming otherwise. **Fixed**: added `nearestZone` (ported from `volley_domain.court.nearest_zone`) to `lib/court-geometry.ts`, extended `matchesStatCategory`/`StatInspectRequest` with an optional `zone` filter, wired `ZoneMap`'s `onZoneClick`/`activeZone` through `StatisticsDashboard` to the existing `onInspect` flow. Verified live: clicked zone 2 on the existing demo match, got exactly 46 matching attack events (matching the displayed count) spanning all 5 sets; clicked zone 1 (count 0), correctly got "No matching events found."
- **No focus-visible ring on interactive court markers** (qa-release-engineer finding #2, dormant until the zone-map fix above activated it): `outline-none` with no `focus-visible:` replacement, unlike every other interactive primitive in the codebase. **Fixed**: added `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring` to both zone and player marker groups.
- **`ROADMAP.md`/`ADR-004` still claimed the JSON blob gets fully removed "this phase"** (architecture-lead's S-7, independently confirmed by qa-release-engineer finding #4 citing the release-gate skill): contradicted `TECH_DEBT.md`'s already-revised scope. **Fixed**: both docs updated with a "Revised 2026-08-29" note restating the real removal condition (a real per-frame position pipeline, Phase 5+).
- **Generator fix's comment made a false claim** (architecture-lead's S-4): claimed ace/serve-error rates "don't shift much" after the `point_winner` fix; measured, the ace rate actually dropped ~4.3x. **Fixed**: comment corrected to state the measured behavior and why, with a note that rebalancing needs real research behind a target rate, not guessed numbers.
- **`RallyTimeline` mislabeled match-absolute time as rally-relative when `video_t_start` is null** (architecture-lead's S-6): defaulted to 0 instead of admitting the value can't be computed. **Fixed**: shows "—" and disables seeking for that action instead.
- **`BlockStats.block_kills` structurally always 0, now visibly surfaced** (qa-release-engineer finding #3, a different field from the already-tracked "blocked attack" entry): `_build_action_chain`'s block sub-path hardcodes `def_outcome = "error"`, never `"point"`. **Documented**, not fixed — new `TECH_DEBT.md` entry (same shape as its sibling), since fixing it means a generator design decision (real block-kill sequences vs. an explicit "not modeled" UI label) that wasn't part of this review-response pass.
- **`OSS_MANIFEST.md`'s D3 entry stale** (qa-release-engineer finding #6): said "not yet used by any component," false since `TacticalCourt` uses it. **Fixed.**

**Deferred, tracked, not fixed this pass** (architecture-lead's S-1/S-2, qa-release-engineer's #5/#7 — real but explicitly lower priority per both reviews): stat click-through duplicates `stats/engine.py`'s filters in hand-written TypeScript and fans out ~208 requests per click (same root cause as the already-tracked `lineage.explain_metric` gap — TECH_DEBT.md); `MatchStatisticsOut` ships untyped `dict`, forcing hand-typed TS interfaces (`schemas.py`'s own "never hand-duplicate" comment, violated since Phase 2 already, not newly introduced); zero component-level tests for the ~10 new React components (only pure-logic `lib/` functions are tested); `RallyTimeline`'s `activeActionId` prop wired one direction only (timeline→replay seek works, replay→timeline highlight doesn't).

**Confirmed sound by architecture-lead, don't re-litigate**: the JSON-blob/replay-data coexistence reasoning; the `(set index, index_in_set)` rally-pairing mechanism (provably collision-free via DB unique constraints); the `point_winner` generator fix (traced every rally-shape branch, empirically zero contradictions across 3292 rallies); org-scoping/multi-tenancy (clean, no findings); no silent TODOs anywhere in the new code. **Confirmed live by qa-release-engineer**: all 10 action-level stat categories' click-through genuinely match `stats/engine.py`'s filters field by field; forced error states recover honestly; mobile viewport renders correctly; no console errors.

**Verification after fixes**: 67/67 domain-py + 27/27 api tests, ruff clean; 39/39 web tests (6 new: `nearestZone` x2, `deriveTeamSides` tiers x3, `ballTrajectoryFullCourt` provenance x1), `pnpm lint`/`pnpm typecheck` clean; live-clicked through the running app against the existing (pre-fix) demo match confirming Overview/Statistics/zone click-through/replay legend all work correctly post-fix. **Not yet done**: a formal final re-review pass by qa-release-engineer/architecture-lead confirming the fixes — this session's self-verification is thorough but isn't a substitute for that, per this project's own definition-of-done.

## Independent review status (Phase 2): both required reviews complete, all blocking findings fixed

**volleyball-domain-analyst review** (rule/statistic correctness) found 5 real issues:
1. A real FIVB Rule 9.3 violation in the synthetic generator (4 consecutive same-team touches) — **fixed**, regression test added across 30 seeds.
2. `tip` actions silently excluded from attack statistics — **fixed** (`_ATTACK_LIKE_ACTION_TYPES`).
3. `AttackStats.direction_counts` mislabeled (measures takeoff position, not landing direction) — **fixed**, renamed to `takeoff_position_counts`.
4. The `blocked`-attack heuristic is real but never exercised by the generator — **documented** in `TECH_DEBT.md`, not fixed (needs either generator changes or `Outcome.detail`-based labeling).
5. Libero modeling (two booleans) is adequate today but insufficient for future rotation-legality validation — **documented** in `TECH_DEBT.md`, correctly not "fixed" with a guessed rule.

**architecture-lead review** (schema/traceability/migration soundness) initially returned **"No — not yet sound"**, with 4 blocking bugs and 4 should-fix gaps:
1. Away-team zone attribution silently wrong (mirrored coordinates written as if unmirrored) — **fixed and reproduced before/after**. The most severe of the four.
2. Rally/Action timestamps were rally-relative, not match-absolute (every rally claimed the same ~0–12s window) — **fixed and reproduced before/after**.
3. Migration 0002 read live ORM metadata at migration-run time instead of freezing DDL — **fixed**: rewritten as frozen `op.create_table()` calls, generated once from metadata and reviewed by hand, no runtime `volley_domain` dependency.
4. `Action.model_run_id` was nullable/`SET NULL`, contrary to CLAUDE.md's provenance requirement and inconsistent with `BallObservation`/`PlayerObservation` — **fixed**: now `NOT NULL`/`CASCADE`.
5. (S1) No uniqueness on ordering columns — **fixed** (4 new `UniqueConstraint`s). Get-or-create for `persist_synthetic_match`'s `Season`/`Team`/`Player`/`Roster` creation — **not fixed**, tracked in `TECH_DEBT.md`.
6. (S2) `lineage.explain_metric` not callable end-to-end by any real caller — **not fixed**, tracked in `TECH_DEBT.md`.
7. (S3) `Video.video_hash` was globally unique instead of org-scoped — **fixed**.
8. (S4) Two "Phase 3" references that should have said "Phase 5" — **fixed**, in `ADR-004`, `ONTOLOGY.md`, `TECH_DEBT.md`.

Full detail for both reviews: `ADR-004`'s "Independent review findings" and "Independent architecture review findings" sections.

## Two real design gaps caught and fixed *during* this implementation (kept visible on purpose)

1. `PipelineRun.video_id` was originally `NOT NULL`, but synthetic pipeline runs have no source video. Rather than fabricate a placeholder `Video` row (which would misrepresent one as existing), made it nullable with a documented single exception (synthetic runs only) — see ADR-004 and the corresponding `TECH_DEBT.md` entry (no DB-level constraint yet enforces the exception; application-code discipline only, until a real pipeline run exists in Phase 5).
2. A first draft of the `/rallies/{id}/actions` endpoint bolted an `outcome` attribute onto `Action` instances dynamically at runtime (`action.outcome = ...`) to avoid a second query — this would have worked but bypassed proper typing/eager-loading. Replaced with a real one-to-one SQLAlchemy `relationship()` between `Action`/`Outcome` plus `selectinload` in the route.

## Open risks carried forward

- **Live-Postgres migration verification: done 2026-08-29** (previously the top open risk — Docker Desktop was broken on this machine for several sessions; root cause was a disabled Windows `WSLService` plus a stuck AF_UNIX socket driver requiring a Winsock reset + reboot, now resolved). Only the downgrade direction remains offline-only-verified — see above.
- **pyright still not run** — not installed in this sandbox, and no network install was attempted this session either. Ruff and the full test suite were run and are clean; static type-checking coverage for this session's changes is an open gap, not a verified-clean claim.
- **No real match video has been run through any pipeline yet** — everything verified so far uses the synthetic generator (`generate_synthetic_match`), which exercises the *ontology/statistics/API* layers but not detection/tracking/pose/court-calibration, none of which exist yet (Phases 4–6). A real video isn't usable for an end-to-end test until those phases ship.
- Deferred, tracked in `TECH_DEBT.md`: `persist_synthetic_match`'s missing get-or-create (duplicate `Team`/`Season`/`Player` rows across repeated demo runs), `lineage.explain_metric` not wired to a real caller, the never-exercised `blocked`-attack heuristic, libero modeling insufficient for future rotation-legality checks.
- The JSON-blob/ontology dual-write in the worker task is real, ongoing-by-design complexity (see `TECH_DEBT.md`, revised 2026-08-29) — Phase 3's frontend now reads discrete stats/events entirely from the real ontology, but the blob's per-rally position time series has no removal condition until Phase 5+ produces real per-frame observations.
- ~~`synthetic/generator.py`'s ace/serve-error rally shapes could produce a `Rally.point_winner_team_id` contradicting the rally's own Action/Outcome~~ — **fixed 2026-08-29**, see Phase 3 section above.
- ~~`MatchOut` doesn't expose `home_team_id`/`away_team_id`; team-side resolution would stop working once real (non-synthetic) matches exist~~ — **fixed 2026-08-29** (`persist_synthetic_match` now links `Match` to its `Team` rows, `MatchOut` exposes both ids, `deriveTeamSides` is a 3-tier fallback that no longer needs the blob for any match going forward). See TECH_DEBT.md's "MatchOut team-id gap" entry.
- Existing demo data in any already-running dev database was generated **before** the `point_winner` fix above and may still contain the inconsistency for rallies created prior to 2026-08-29 — not a concern for a fresh `docker compose` environment, but worth knowing if debugging against a long-lived local database.
- Carried from Phase 0/1: RF-DETR XL/2XL licensing (D-002), FFmpeg build discipline (D-006, open), JWT 15-minute revocation gap (accepted for synthetic-data-only stakes, revisit before Phase 7).
- **Remote configured 2026-08-29**: `origin` → `https://github.com/franiatesting-tech/VolleyAnalitics.git`, `main` branch pushed (Phase 0 through Phase 2 history). `.github/workflows/ci.yml` already existed from Phase 1 (python lint/typecheck/pytest/migration-check, web lint/typecheck/vitest/build, Playwright e2e smoke) and should now run for real on GitHub for the first time — not yet confirmed green, since this sandbox has no `gh` CLI/network access to check Actions results.

## Immediate next steps

1. Push these fixes to the remote so the next CI run reflects Phase 3's real state, and confirm it's green (this sandbox can't check Actions status directly — ask the user to look, or wire up `gh` CLI access). Should also reveal whether the new e2e smoke test (rewritten and re-checked this session, but never actually executed end-to-end here — the underlying UI flow it asserts on was independently verified live by both reviewers, just not through Playwright itself) actually passes in CI, and whether `packages/ui`'s missing `package.json` (fixed this session) was silently failing `web`/`web-migrate`'s Docker build in CI too.
2. Minor, non-blocking: qa-release-engineer's confirmation pass saw two `Failed to load resource: 404` console entries once, couldn't reproduce them on a fresh navigation, and suspects dev-server/HMR noise (Turbopack was actively Fast-Refreshing) rather than a real regression. Worth a quick look before a real release, not blocking Phase 4.
3. **Phase 4 (dataset factory, annotation, golden set) is next in ROADMAP.md order — Phase 3 is confirmed done.**

## How to keep this file honest

Update it at the end of any significant session: what changed, what was verified, what's blocked, what's next. This is a status snapshot, not a changelog — prune stale entries rather than accumulating history (git history is the changelog).
